# Data

- `raw/`: immutable source data dumps
- `processed/`: cleaned datasets and feature tables
- `external/`: third-party datasets

These folders are checked into Git with `.gitkeep` placeholder files so the structure exists before data is added.
